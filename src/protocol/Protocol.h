#ifndef CS_NETWORK_PROTOCOL_H
#define CS_NETWORK_PROTOCOL_H

// 请求包类型
enum RequestType {
    COMMAND = 0,
};

// 回复包类型
enum ResponseType {
    PLAIN_TEXT = 0,
};

class Protocol {

};


#endif //CS_NETWORK_PROTOCOL_H
